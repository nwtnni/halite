pub struct Ship {
    pub owner: usize,
    pub id: usize,
    pub x: usize,
    pub y: usize,
    pub halite: usize,
}

pub struct Shipyard {
    pub owner: usize,
    pub id: usize,
    pub x: usize,
    pub y: usize,
}

pub struct Dropoff {
    pub owner: usize,
    pub id: usize,
    pub x: usize,
    pub y: usize,
}
